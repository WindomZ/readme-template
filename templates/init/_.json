{
  "input": {
    "user": {
      "name": "${user}",
      "description": "git user:",
      "type": "string",
      "default": ""
    },
    "repo": {
      "name": "${repo}",
      "description": "git repository:",
      "type": "string",
      "default": ""
    },
    "repo-url": {
      "name": "${repo-url}",
      "description": "git repository URL:",
      "type": "string",
      "default": ""
    },
    "npm": {
      "name": "${npm}",
      "description": "npm:",
      "type": "string",
      "default": ""
    }
  }
}